"""Runnable examples for the moondb SDK.

See ``pilotspace_example.py`` for a full PilotSpace-shaped demo covering
text search, aggregate faceting, and hybrid retrieval.

Examples are importable as a package (``from examples.pilotspace_example
import run_demo``) and runnable as scripts (``uv run python
examples/pilotspace_example.py --dry-run``).
"""
