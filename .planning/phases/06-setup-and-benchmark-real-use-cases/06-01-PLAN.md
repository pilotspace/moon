---
phase: 06-setup-and-benchmark-real-use-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - Dockerfile
  - .dockerignore
  - .github/workflows/ci.yml
  - Cargo.toml
autonomous: true
requirements:
  - SETUP-01

must_haves:
  truths:
    - "README.md documents all features, CLI flags, build instructions, and quick start"
    - "Dockerfile builds successfully with docker build and produces a runnable image"
    - "CI config runs cargo test, cargo clippy, and cargo fmt --check on push/PR"
    - "Cargo.toml has description, license, keywords, and categories metadata"
  artifacts:
    - path: "README.md"
      provides: "Project documentation with features, usage, build instructions"
      contains: "cargo build --release"
    - path: "Dockerfile"
      provides: "Multi-stage build producing slim runtime image"
      contains: "FROM rust"
    - path: ".dockerignore"
      provides: "Excludes target/, .git/, .planning/ from Docker context"
      contains: "target/"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline with test, clippy, fmt jobs"
      contains: "cargo test"
    - path: "Cargo.toml"
      provides: "Package metadata fields"
      contains: "description"
  key_links:
    - from: "Dockerfile"
      to: "Cargo.toml"
      via: "cargo build --release copies binary"
      pattern: "cargo build --release"
    - from: ".github/workflows/ci.yml"
      to: "Cargo.toml"
      via: "cargo test runs project tests"
      pattern: "cargo test"
---

<objective>
Project packaging: README, Dockerfile, CI config, and Cargo.toml metadata.

Purpose: Make the project presentable, buildable via Docker, and automatically tested via CI.
Output: README.md, Dockerfile, .dockerignore, .github/workflows/ci.yml, updated Cargo.toml
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-setup-and-benchmark-real-use-cases/06-CONTEXT.md
@.planning/phases/06-setup-and-benchmark-real-use-cases/06-RESEARCH.md
@src/config.rs
@src/main.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md, Dockerfile, .dockerignore, and CI config</name>
  <files>README.md, Dockerfile, .dockerignore, .github/workflows/ci.yml</files>
  <read_first>
    - src/config.rs (all CLI flags for usage documentation)
    - src/main.rs (entry point for quick start docs)
    - Cargo.toml (project name, dependencies)
    - .planning/phases/06-setup-and-benchmark-real-use-cases/06-RESEARCH.md (exact patterns for Dockerfile, CI, README)
    - .planning/phases/06-setup-and-benchmark-real-use-cases/06-CONTEXT.md (locked decisions, command list)
  </read_first>
  <action>
Create four files:

**README.md** with these sections:
1. Title: "rust-redis" with one-line description: "A high-performance Redis-compatible server written in Rust"
2. Features: List all implemented command families from CONTEXT.md:
   - Connection (7): PING, ECHO, QUIT, SELECT, COMMAND, INFO, AUTH
   - Strings (17): GET, SET, MGET, MSET, INCR, DECR, INCRBY, DECRBY, INCRBYFLOAT, APPEND, STRLEN, SETNX, SETEX, PSETEX, GETSET, GETDEL, GETEX
   - Keys (13): DEL, EXISTS, EXPIRE, PEXPIRE, TTL, PTTL, PERSIST, TYPE, UNLINK, SCAN, KEYS, RENAME, RENAMENX
   - Hashes (14): HSET, HGET, HDEL, HMSET, HMGET, HGETALL, HEXISTS, HLEN, HKEYS, HVALS, HINCRBY, HINCRBYFLOAT, HSETNX, HSCAN
   - Lists (12): LPUSH, RPUSH, LPOP, RPOP, LLEN, LRANGE, LINDEX, LSET, LINSERT, LREM, LTRIM, LPOS
   - Sets (15): SADD, SREM, SMEMBERS, SCARD, SISMEMBER, SMISMEMBER, SINTER, SUNION, SDIFF, SINTERSTORE, SUNIONSTORE, SDIFFSTORE, SRANDMEMBER, SPOP, SSCAN
   - Sorted Sets (18): ZADD, ZREM, ZSCORE, ZCARD, ZINCRBY, ZRANK, ZREVRANK, ZPOPMIN, ZPOPMAX, ZSCAN, ZRANGE, ZREVRANGE, ZRANGEBYSCORE, ZREVRANGEBYSCORE, ZCOUNT, ZLEXCOUNT, ZUNIONSTORE, ZINTERSTORE
   - Pub/Sub (4): SUBSCRIBE, UNSUBSCRIBE, PSUBSCRIBE, PUBLISH
   - Transactions (5): MULTI, EXEC, DISCARD, WATCH, UNWATCH
   - Persistence (2): BGSAVE, BGREWRITEAOF
   - Config (2): CONFIG GET, CONFIG SET
3. Quick Start:
   ```
   cargo build --release
   ./target/release/rust-redis
   ```
   Then connect with `redis-cli`.
4. Configuration: Table of all CLI flags from config.rs (flag, default, description)
5. Docker section:
   ```
   docker build -t rust-redis .
   docker run -p 6379:6379 rust-redis
   ```
6. Known Limitations section: No cluster mode, no Lua scripting, no Redis Streams, no RESP3, no replication
7. License: MIT

**Dockerfile** (multi-stage, per research pattern):
- Stage 1: `FROM rust:1.85-bookworm AS builder`, WORKDIR /app, COPY Cargo.toml Cargo.lock src/ benches/, `cargo build --release`
- Stage 2: `FROM debian:bookworm-slim`, install ca-certificates, create non-root user `rustredis`, COPY binary from builder, EXPOSE 6379, CMD `["rust-redis", "--bind", "0.0.0.0"]`

**.dockerignore**:
```
target/
.git/
.github/
*.md
.planning/
benches/
tests/
```

**.github/workflows/ci.yml** (per research pattern):
- name: CI
- on: push to main, pull_request to main
- Three parallel jobs: test (cargo test --all-features), clippy (cargo clippy -- -D warnings), fmt (cargo fmt --check)
- Each job: actions/checkout@v4, dtolnay/rust-toolchain@stable (with components for clippy/fmt), Swatinem/rust-cache@v2
  </action>
  <verify>
    <automated>test -f README.md && test -f Dockerfile && test -f .dockerignore && test -f .github/workflows/ci.yml && echo "All files exist"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "cargo build --release"
    - README.md contains "Known Limitations"
    - README.md contains all CLI flags (--bind, --port, --requirepass, --appendonly, --maxmemory, --maxmemory-policy)
    - Dockerfile contains "FROM rust" and "FROM debian:bookworm-slim"
    - Dockerfile contains "rust-redis" and "--bind" and "0.0.0.0"
    - .dockerignore contains "target/" and ".planning/"
    - ci.yml contains "cargo test" and "cargo clippy" and "cargo fmt"
    - ci.yml contains "dtolnay/rust-toolchain@stable"
  </acceptance_criteria>
  <done>README.md documents all features/flags/usage, Dockerfile builds multi-stage, CI config has 3 jobs, .dockerignore excludes build artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Update Cargo.toml metadata fields</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current state to preserve all existing content)
  </read_first>
  <action>
Add metadata fields to the [package] section of Cargo.toml (preserve all existing fields and dependencies):

```toml
description = "A high-performance Redis-compatible server written in Rust"
license = "MIT"
repository = "https://github.com/tindang/rust-redis"
keywords = ["redis", "database", "cache", "server"]
categories = ["database", "network-programming"]
```

Also create a LICENSE file with the MIT license text, copyright 2026.

Verify the metadata is valid by running `cargo metadata --format-version=1 2>&1 | head -1` to confirm no errors.
  </action>
  <verify>
    <automated>cargo metadata --format-version=1 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); p=[x for x in d['packages'] if x['name']=='rust-redis'][0]; assert p['description'] is not None; assert p['license'] is not None; print('Metadata valid')"</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains 'description = "A high-performance Redis-compatible server written in Rust"'
    - Cargo.toml contains 'license = "MIT"'
    - Cargo.toml contains 'keywords'
    - LICENSE file exists
    - cargo metadata succeeds without errors
  </acceptance_criteria>
  <done>Cargo.toml has description, license, repository, keywords, categories. LICENSE file exists. cargo metadata validates successfully.</done>
</task>

</tasks>

<verification>
- All 5 new files exist: README.md, Dockerfile, .dockerignore, .github/workflows/ci.yml, LICENSE
- Cargo.toml metadata fields present
- `cargo test` still passes (no regressions)
- `cargo metadata` shows description and license
</verification>

<success_criteria>
- README.md comprehensively documents the project with features, usage, CLI flags, Docker, and known limitations
- Dockerfile uses multi-stage build with non-root user
- CI config covers test + clippy + fmt
- Cargo.toml metadata is complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-setup-and-benchmark-real-use-cases/06-01-SUMMARY.md`
</output>
