---
phase: 31-full-monoio-migration
plan: 04
type: execute
wave: 3
depends_on: [31-02, 31-03]
files_modified:
  - src/runtime/mod.rs
  - src/io/mod.rs
  - Cargo.toml
autonomous: true
requirements: [MONOIO-FULL-04]

must_haves:
  truths:
    - "cargo build --no-default-features --features runtime-monoio compiles with zero errors"
    - "cargo build (default features, runtime-tokio) compiles with zero errors"
    - "cargo test (default features) passes all 1134+ tests"
    - "No unconditional tokio/tokio_util imports exist outside cfg blocks"
  artifacts:
    - path: "Cargo.toml"
      provides: "Correct feature flags and dependencies for dual-runtime"
  key_links:
    - from: "Cargo.toml"
      to: "src/runtime/mod.rs"
      via: "runtime-tokio and runtime-monoio features"
      pattern: "runtime-tokio.*runtime-monoio"
---

<objective>
Fix all remaining compile errors for monoio build, verify both runtimes compile, and run full test suite under tokio.

Purpose: This is the integration gate -- the binary must compile under both features and all tests must pass under the default runtime.
Output: Zero-error monoio build, passing tokio test suite.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-full-monoio-migration-replace-tokio-channels-select-tcp-codec-spawn-across-30-files-for-complete-runtime-swap/31-01-SUMMARY.md
@.planning/phases/31-full-monoio-migration-replace-tokio-channels-select-tcp-codec-spawn-across-30-files-for-complete-runtime-swap/31-02-SUMMARY.md
@.planning/phases/31-full-monoio-migration-replace-tokio-channels-select-tcp-codec-spawn-across-30-files-for-complete-runtime-swap/31-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining monoio compile errors and verify dual-runtime build</name>
  <files>Any files still producing errors under runtime-monoio (discovered at execution time)</files>
  <read_first>
    - Run `cargo build --no-default-features --features runtime-monoio 2>&1 | head -100` to see remaining errors
    - src/runtime/mod.rs (current module structure)
    - src/io/mod.rs (io module structure for cfg-gating)
    - Cargo.toml (dependency/feature state)
  </read_first>
  <action>
1. Run `cargo build --no-default-features --features runtime-monoio` and collect all remaining errors.

2. For each error, apply the appropriate fix:
   - **Unresolved import**: Add `#[cfg(feature = "runtime-tokio")]` guard or replace with runtime-agnostic alternative
   - **Missing type**: Add cfg-gated type alias or stub for monoio path
   - **Trait not implemented**: Check if monoio types need different trait bounds
   - **Select macro**: Ensure all `tokio::select!` sites are properly cfg-gated

3. Common remaining issues to check:
   - `src/runtime/mod.rs` line 2: may still have unconditional re-exports that reference tokio types
   - Test files (`tests/integration.rs`, `tests/replication_test.rs`) -- these use `#[tokio::test]` and tokio types, which is fine since tests only run under runtime-tokio
   - `src/pubsub/mod.rs` line 234: test module uses `tokio::sync::mpsc` -- fine under `#[cfg(test)]` since tests run with tokio
   - io_uring driver (`src/io/uring_driver.rs` or similar) -- check if it has tokio deps

4. Handle monoio-specific AsyncRead/AsyncWrite differences:
   - monoio uses its own `monoio::io::AsyncReadRent`/`AsyncWriteRent` (rent-based, not standard AsyncRead/AsyncWrite)
   - For files that use `tokio::io::{AsyncReadExt, AsyncWriteExt}`, the monoio cfg path needs monoio's equivalent
   - In cluster/bus, gossip, replication -- the TCP read/write code may need significant cfg-gating because monoio's I/O model is fundamentally different (buffer ownership transfer)
   - Strategy: cfg-gate entire async function bodies that do TCP I/O, with tokio version using AsyncRead/WriteExt and monoio version using AsyncReadRent/AsyncWriteRent

5. Verify: `cargo build --no-default-features --features runtime-monoio` produces zero errors.

6. Verify: `cargo build` (default features) still compiles cleanly.

7. Verify: `cargo test` (default features) passes all tests. Run full test suite:
   ```
   cargo test 2>&1 | tail -20
   ```
   All 1134+ tests must pass.

8. If any test failures, fix them. Common issues:
   - Channel API differences (`.recv()` vs `.recv_async()`)
   - CancellationToken behavior differences
   - Import paths changed

9. Final cleanup: `cargo clippy` to catch any warnings from the migration.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/rust-redis && cargo build --no-default-features --features runtime-monoio 2>&1 | tail -5 && cargo build 2>&1 | tail -5 && cargo test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --no-default-features --features runtime-monoio` exits with 0 errors
    - `cargo build` (default features) exits with 0 errors
    - `cargo test` passes all tests (1134+ tests, 0 failures)
    - No unconditional tokio/tokio_util imports in src/ files (outside cfg blocks and test modules)
  </acceptance_criteria>
  <done>Both runtime-tokio and runtime-monoio compile cleanly; full test suite passes</done>
</task>

</tasks>

<verification>
- `cargo build --no-default-features --features runtime-monoio` succeeds (zero errors)
- `cargo build` succeeds (zero errors)
- `cargo test` passes all tests
- `grep -rn "use tokio" src/ --include="*.rs" | grep -v "cfg\|test\|//"` shows no ungated imports
</verification>

<success_criteria>
- Dual-runtime compilation verified
- Full test suite regression-free
- Phase 31 goal achieved: `runtime-monoio` feature produces a compilable binary
</success_criteria>

<output>
After completion, create `.planning/phases/31-full-monoio-migration-replace-tokio-channels-select-tcp-codec-spawn-across-30-files-for-complete-runtime-swap/31-04-SUMMARY.md`
</output>
