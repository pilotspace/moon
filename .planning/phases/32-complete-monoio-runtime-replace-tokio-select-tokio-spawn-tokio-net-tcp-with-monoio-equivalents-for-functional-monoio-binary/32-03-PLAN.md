---
phase: 32-complete-monoio-runtime
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - src/server/connection.rs
  - src/shard/mod.rs
  - src/main.rs
  - src/server/listener.rs
autonomous: false
requirements:
  - MONOIO-06

must_haves:
  truths:
    - "cargo build --no-default-features --features runtime-monoio produces a working binary"
    - "Binary starts and listens on configured port"
    - "redis-cli PING returns PONG"
    - "redis-cli SET foo bar returns OK"
    - "redis-cli GET foo returns bar"
    - "cargo test --features runtime-tokio still passes (no regressions)"
  artifacts:
    - path: "target/debug/rust-redis"
      provides: "Working Monoio binary"
  key_links:
    - from: "redis-cli"
      to: "monoio binary"
      via: "TCP on port 6400"
      pattern: "PING|SET|GET"
---

<objective>
Build the monoio binary, fix any remaining compilation issues, and verify SET/GET/PING work end-to-end with redis-cli.

Purpose: Final integration verification. The previous plans implemented all the pieces; this plan ensures they work together as a functional server.

Output: Working monoio binary that handles basic Redis commands.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-complete-monoio-runtime-replace-tokio-select-tokio-spawn-tokio-net-tcp-with-monoio-equivalents-for-functional-monoio-binary/32-01-SUMMARY.md
@.planning/phases/32-complete-monoio-runtime-replace-tokio-select-tokio-spawn-tokio-net-tcp-with-monoio-equivalents-for-functional-monoio-binary/32-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build monoio binary and fix remaining issues</name>
  <files>src/server/connection.rs, src/shard/mod.rs, src/main.rs, src/server/listener.rs</files>
  <read_first>cargo build output (run build first)</read_first>
  <action>
1. Run `cargo build --no-default-features --features runtime-monoio` and fix any remaining compilation errors.

   Common issues to expect and how to fix:
   - **Missing imports under cfg(runtime-monoio)**: Add appropriate use statements
   - **monoio::net::TcpStream IntoRawFd**: May need `use std::os::unix::io::{IntoRawFd, FromRawFd}` with cfg(unix)
   - **BytesMut not implementing monoio IoBuf**: Use `Vec<u8>` intermediate for reads, `Bytes`/`Vec<u8>` for writes
   - **Framed/tokio-util references leaking into monoio paths**: Ensure all Framed usage is behind `#[cfg(feature = "runtime-tokio")]`
   - **dispatch() signature mismatches**: Check exact dispatch() parameters and adjust connection handler call
   - **Type mismatches in select! arms**: Ensure flume async recv returns correct types

2. After fixing, also run `cargo check --features runtime-tokio` to verify no regressions.

3. Run `cargo test --features runtime-tokio` to verify existing tests still pass.

4. Run `cargo clippy --no-default-features --features runtime-monoio -- -D warnings 2>&1 | head -30` and fix any warnings that would cause issues.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/rust-redis && cargo build --no-default-features --features runtime-monoio 2>&1 | tail -5 && cargo test --features runtime-tokio 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo build --no-default-features --features runtime-monoio succeeds
    - cargo test --features runtime-tokio passes with 0 failures
    - No new clippy warnings under monoio feature
  </acceptance_criteria>
  <done>Monoio binary compiles and tokio tests still pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test -- redis-cli SET/GET/PING against monoio binary</name>
  <files>target/debug/rust-redis</files>
  <action>
Start the monoio binary and verify basic Redis commands work via redis-cli. Run:
`cargo run --no-default-features --features runtime-monoio -- --port 6400 --shards 1`
Then test PING, SET, GET, INCR, DEL from redis-cli on port 6400.
Stop with Ctrl+C and verify clean shutdown.
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/rust-redis && cargo build --no-default-features --features runtime-monoio 2>&1 | tail -3</automated>
  </verify>
  <done>redis-cli PING returns PONG, SET foo bar returns OK, GET foo returns bar, Ctrl+C shuts down cleanly</done>
  <what-built>Complete monoio runtime binary that should handle basic Redis commands</what-built>
  <how-to-verify>
1. Start the monoio binary:
   ```
   cargo run --no-default-features --features runtime-monoio -- --port 6400 --shards 1
   ```
   Expect to see: "Starting with 1 shards" and "Listening on 0.0.0.0:6400"

2. In another terminal, test with redis-cli:
   ```
   redis-cli -p 6400 PING
   # Expected: PONG

   redis-cli -p 6400 SET foo bar
   # Expected: OK

   redis-cli -p 6400 GET foo
   # Expected: "bar"

   redis-cli -p 6400 SET counter 0
   redis-cli -p 6400 INCR counter
   # Expected: (integer) 1

   redis-cli -p 6400 DEL foo counter
   # Expected: (integer) 2
   ```

3. Stop the server with Ctrl+C. Expect clean shutdown message.
  </how-to-verify>
  <resume-signal>Type "approved" if SET/GET/PING work, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Monoio binary compiles and starts
- PING/SET/GET work via redis-cli
- Ctrl+C triggers clean shutdown
- Tokio path unaffected (tests pass)
</verification>

<success_criteria>
End-to-end functional monoio binary: `cargo run --no-default-features --features runtime-monoio -- --port 6400 --shards 1` starts a server that handles redis-cli SET/GET/PING commands correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/32-complete-monoio-runtime-replace-tokio-select-tokio-spawn-tokio-net-tcp-with-monoio-equivalents-for-functional-monoio-binary/32-03-SUMMARY.md`
</output>
