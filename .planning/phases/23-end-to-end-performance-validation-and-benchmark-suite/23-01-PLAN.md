---
phase: 23-end-to-end-performance-validation-and-benchmark-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench-v2.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "bench-v2.sh --smoke-test --rust-only runs without error on dev hardware (macOS or Linux)"
    - "Server starts, 1K keys are pre-populated using memtier_benchmark --key-pattern P:P, primary throughput test runs for 10 seconds, server stops cleanly"
    - "Results directory is created with raw memtier output files and an HDR histogram prefix file"
    - "Script detects missing memtier_benchmark and prints install instructions rather than crashing"
    - "Script builds the rust-redis release binary if it is not present"
  artifacts:
    - path: "bench-v2.sh"
      provides: "Main v2 benchmark runner with server lifecycle, pre-population, and primary throughput test"
      exports: ["--smoke-test", "--rust-only", "--redis-only", "--server-host", "--output"]
  key_links:
    - from: "bench-v2.sh run_prepopulate()"
      to: "memtier_benchmark --key-pattern P:P --ratio 1:0"
      via: "sequential key loading ensuring all N keys exist before benchmark"
      pattern: "key-pattern=P:P.*ratio=1:0"
    - from: "bench-v2.sh run_benchmark_v2()"
      to: "memtier_benchmark --key-pattern Z:Z --run-count 5"
      via: "Zipfian read-heavy workload, 5 iterations"
      pattern: "key-pattern=Z:Z"
---

<objective>
Create bench-v2.sh: the primary benchmark runner for Phase 23. This plan covers the core script structure — server lifecycle management, 10M-key pre-population, and the primary throughput test loop. A --smoke-test mode (1K keys, 10s test) makes validation fast without full 180s runs.

Purpose: The benchmark infrastructure is the primary deliverable of Phase 23. Without bench-v2.sh, the methodology cannot be reproduced on any hardware.
Output: bench-v2.sh — executable, self-documenting, smoke-testable.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-end-to-end-performance-validation-and-benchmark-suite/23-CONTEXT.md
@.planning/phases/23-end-to-end-performance-validation-and-benchmark-suite/23-RESEARCH.md
@bench.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bench-v2.sh core (server lifecycle + pre-population + primary throughput test)</name>
  <files>bench-v2.sh</files>

  <read_first>
    - bench.sh: inherit start_rust_server(), start_redis_server(), wait_for_server(), stop_rust_server(), stop_redis_server(), cleanup trap, log() helper, check_rust_binary(), get_rss() patterns
    - 23-RESEARCH.md §Pattern 1 (bench-v2.sh Architecture): exact memtier_benchmark flag sets for pre-population and primary benchmark
    - 23-RESEARCH.md §Pitfall 2: pre-populate with P:P not Z:Z
    - 23-RESEARCH.md §Pitfall 1: always set --key-maximum=10000000
    - 23-CONTEXT.md §Workload configuration: Zipfian alpha 0.99, GET:SET 1:10, 180s test, 30s warmup, 5 iterations, pipeline depths 1/16/64, concurrency 1/10/50/100
  </read_first>

  <action>
Create bench-v2.sh with the following structure. Make the file executable (chmod +x).

**Header and configuration block:**
```bash
#!/usr/bin/env bash
set -euo pipefail

###############################################################################
# bench-v2.sh -- Phase 23: End-to-End Performance Validation Benchmark Suite
#
# Primary tool: memtier_benchmark (industry standard, HdrHistogram, Zipfian)
# Do NOT use redis-benchmark for primary results (lacks Zipfian + rate-limiting)
#
# Usage:
#   ./bench-v2.sh                         # Full suite: rust-redis + Redis 7.x comparison
#   ./bench-v2.sh --smoke-test            # Fast validation: 1K keys, 10s runs
#   ./bench-v2.sh --rust-only             # Skip Redis 7.x comparison
#   ./bench-v2.sh --redis-only            # Only benchmark Redis 7.x
#   ./bench-v2.sh --memory-only           # Memory overhead section only
#   ./bench-v2.sh --snapshot-only         # Snapshot spike section only
#   ./bench-v2.sh --open-loop-only        # Open-loop rate-limited section only
#   ./bench-v2.sh --server-host HOST      # Point at remote server (for target hardware)
#   ./bench-v2.sh --output FILE           # Write BENCHMARK-v2.md to FILE
#   ./bench-v2.sh --keys N                # Override key count (default: 10000000)
#   ./bench-v2.sh --test-time N           # Override test duration in seconds (default: 180)
#
# Methodology:
#   - Pre-populate with P:P (sequential) to ensure ALL keys are loaded
#   - Benchmark with Z:Z (Zipfian alpha ~0.99) to simulate real-world hot-spot access
#   - 5 iterations per configuration; report median ops/sec with min/max range
#   - Open-loop (--rate-limiting) tests for coordinated-omission-aware latency curves
#   - Compare against Redis 7.x and N-shard Redis simulation (same machine, same core count)
#   - Client co-location caveat documented when client runs on same machine as server
###############################################################################
```

**Configuration variables:**
```bash
PORT_RUST=6399
PORT_REDIS=6400
REDIS_CLUSTER_BASE_PORT=7000

# Benchmark parameters (overridden by --smoke-test)
KEY_MAX=10000000
TEST_TIME=180
WARMUP_TIME=30
RUN_COUNT=5
THREADS=4
CLIENTS_LIST=(1 10 50 100)
PIPELINE_LIST=(1 16 64)
DATA_SIZE=256

# Smoke test overrides
SMOKE_TEST=false
SMOKE_KEY_MAX=1000
SMOKE_TEST_TIME=10
SMOKE_WARMUP_TIME=5
SMOKE_RUN_COUNT=1

# Flags
RUN_RUST=true
RUN_REDIS=true
RUN_MEMORY=true
RUN_SNAPSHOT=true
RUN_OPEN_LOOP=true
SERVER_HOST="127.0.0.1"
OUTPUT_FILE="BENCHMARK-v2.md"
RESULTS_DIR=""

RUST_BINARY="./target/release/rust-redis"
RUST_PID=""
REDIS_PID=""
REDIS_CLUSTER_PIDS=()
```

**Argument parsing (parse_args function):**
Parse: --smoke-test, --rust-only, --redis-only, --memory-only, --snapshot-only, --open-loop-only, --server-host HOST, --output FILE, --keys N, --test-time N

When --smoke-test is set, override KEY_MAX, TEST_TIME, WARMUP_TIME, RUN_COUNT with smoke values.
When --memory-only: set RUN_SNAPSHOT=false RUN_OPEN_LOOP=false, skip primary throughput.
When --snapshot-only: set RUN_MEMORY=false RUN_OPEN_LOOP=false, skip primary throughput.
When --open-loop-only: set RUN_MEMORY=false RUN_SNAPSHOT=false, skip primary throughput.

**Helper functions (inherit from bench.sh patterns):**
```bash
log() { echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" >&2; }

cleanup() {
    log "Cleaning up..."
    # Kill rust-redis if started by this script
    if [[ -n "${RUST_PID:-}" ]] && kill -0 "$RUST_PID" 2>/dev/null; then
        kill "$RUST_PID" 2>/dev/null || true; wait "$RUST_PID" 2>/dev/null || true
    fi
    # Kill redis comparison instance
    if [[ -n "${REDIS_PID:-}" ]] && kill -0 "$REDIS_PID" 2>/dev/null; then
        kill "$REDIS_PID" 2>/dev/null || true; wait "$REDIS_PID" 2>/dev/null || true
    fi
    # Kill Redis cluster simulation instances
    for pid in "${REDIS_CLUSTER_PIDS[@]:-}"; do
        kill "$pid" 2>/dev/null || true
    done
    # Keep RESULTS_DIR: raw data is useful for offline analysis
}
trap cleanup EXIT

check_memtier() {
    if ! command -v memtier_benchmark &>/dev/null; then
        echo "ERROR: memtier_benchmark is not installed." >&2
        echo "Install:" >&2
        echo "  macOS:  brew install memtier_benchmark" >&2
        echo "  Ubuntu: sudo apt-get install -y memtier-benchmark" >&2
        echo "  Source: git clone https://github.com/redis/memtier_benchmark.git" >&2
        echo "          cd memtier_benchmark && autoreconf -ivf && ./configure && make && sudo make install" >&2
        exit 1
    fi
    log "memtier_benchmark: $(memtier_benchmark --version 2>&1 | head -1)"
}

check_redis_cli() {
    if ! command -v redis-cli &>/dev/null; then
        log "WARNING: redis-cli not found. Memory measurements will be skipped."
        RUN_MEMORY=false
        RUN_SNAPSHOT=false
    fi
}

check_rust_binary() {
    if [[ ! -x "$RUST_BINARY" ]]; then
        log "rust-redis binary not found. Building with: cargo build --release"
        cargo build --release
    fi
}

wait_for_server() {
    local port=$1
    local max_attempts=100
    local attempt=0
    while ! redis-cli -p "$port" PING &>/dev/null 2>&1; do
        attempt=$((attempt + 1))
        if [[ $attempt -ge $max_attempts ]]; then
            log "ERROR: Server on port $port failed to start within 10 seconds"
            return 1
        fi
        sleep 0.1
    done
    log "Server ready on port $port"
}

get_rss() {
    local pid=$1
    if [[ "$(uname)" == "Linux" ]]; then
        awk '/VmRSS:/{print $2}' "/proc/$pid/status" 2>/dev/null || echo 0
    else
        ps -o rss= -p "$pid" 2>/dev/null | tr -d ' ' || echo 0
    fi
}

start_rust_server() {
    local port=$1
    log "Starting rust-redis on port $port (shards=$(nproc 2>/dev/null || sysctl -n hw.logicalcpu))"
    "$RUST_BINARY" --port "$port" &
    RUST_PID=$!
    wait_for_server "$port"
}

stop_rust_server() {
    if [[ -n "${RUST_PID:-}" ]] && kill -0 "$RUST_PID" 2>/dev/null; then
        log "Stopping rust-redis (PID $RUST_PID)"
        kill "$RUST_PID" 2>/dev/null || true
        wait "$RUST_PID" 2>/dev/null || true
        RUST_PID=""
    fi
}

start_redis_server() {
    local port=$1
    log "Starting redis-server on port $port"
    redis-server --port "$port" --save "" --daemonize no &
    REDIS_PID=$!
    wait_for_server "$port"
}

stop_redis_server() {
    if [[ -n "${REDIS_PID:-}" ]] && kill -0 "$REDIS_PID" 2>/dev/null; then
        log "Stopping redis-server (PID $REDIS_PID)"
        kill "$REDIS_PID" 2>/dev/null || true
        wait "$REDIS_PID" 2>/dev/null || true
        REDIS_PID=""
    fi
}
```

**Pre-population function:**
```bash
run_prepopulate() {
    local port=$1 label=$2
    log "Pre-populating $label with $KEY_MAX keys (${DATA_SIZE}B values, sequential P:P)..."
    # MUST use P:P (sequential) not Z:Z (Zipfian) for pre-population.
    # Zipfian concentrates on a subset -- using it here would leave most keys absent.
    memtier_benchmark -s "$SERVER_HOST" -p "$port" \
        -t 4 -c 30 \
        --ratio=1:0 \
        --key-pattern=P:P \
        --key-maximum="$KEY_MAX" \
        -d "$DATA_SIZE" \
        -n allkeys \
        --hide-histogram \
        2>&1 | grep -E "^\[|Ops|RPS|KB/sec" >&2 || true
    log "Pre-population complete. Verifying key count..."
    local actual_keys
    actual_keys=$(redis-cli -p "$port" DBSIZE 2>/dev/null || echo "unknown")
    log "Keys loaded: $actual_keys (target: $KEY_MAX)"
}
```

**Primary throughput benchmark function:**
```bash
run_benchmark_v2() {
    local port=$1 label=$2 clients=$3 pipeline=$4
    local outprefix="$RESULTS_DIR/${label}_c${clients}_p${pipeline}"
    log "  Benchmarking $label: clients=$clients pipeline=$pipeline data=${DATA_SIZE}B"
    # Z:Z = Zipfian distribution (hot-spot access, matches real-world workloads)
    # --run-count=5: 5 iterations, memtier reports median
    # --warmup-time: excluded from stats
    # --distinct-client-seed: different clients use different random seeds
    memtier_benchmark -s "$SERVER_HOST" -p "$port" \
        -t "$THREADS" -c "$clients" \
        --ratio=1:10 \
        --key-pattern=Z:Z \
        --key-maximum="$KEY_MAX" \
        -d "$DATA_SIZE" \
        --test-time="$TEST_TIME" \
        --warmup-time="$WARMUP_TIME" \
        --distinct-client-seed \
        --run-count="$RUN_COUNT" \
        --pipeline="$pipeline" \
        --print-percentiles 50,90,95,99,99.9,99.99 \
        --hdr-file-prefix="${outprefix}_hdr" \
        > "${outprefix}.txt" 2>&1
    log "  Done: ${outprefix}.txt"
}
```

**Primary benchmark sweep (run_throughput_suite function):**
```bash
run_throughput_suite() {
    local port=$1 label=$2
    log "=== Throughput suite: $label ==="
    run_prepopulate "$port" "$label"
    for clients in "${CLIENTS_LIST[@]}"; do
        for pipeline in "${PIPELINE_LIST[@]}"; do
            run_benchmark_v2 "$port" "$label" "$clients" "$pipeline"
        done
    done
}
```

**Result extraction helper (extract_ops_sec):**
```bash
# Extract total ops/sec from memtier output file (last "Totals" line)
extract_ops_sec() {
    local file=$1
    grep -m1 "^Totals" "$file" 2>/dev/null | awk '{print $2}' || echo "N/A"
}

# Extract p99 latency (ms) from memtier output file
extract_p99() {
    local file=$1
    grep -m1 "^Totals" "$file" 2>/dev/null | awk '{print $9}' || echo "N/A"
}

# Extract p99.9 latency (ms) from memtier output file
extract_p999() {
    local file=$1
    grep -m1 "^Totals" "$file" 2>/dev/null | awk '{print $10}' || echo "N/A"
}
```

**Main function (main):**
```bash
main() {
    parse_args "$@"
    RESULTS_DIR=$(mktemp -d "/tmp/bench-v2-XXXXXX")
    log "Results directory: $RESULTS_DIR"
    log "Configuration: KEY_MAX=$KEY_MAX TEST_TIME=${TEST_TIME}s WARMUP=${WARMUP_TIME}s RUN_COUNT=$RUN_COUNT"
    [[ "$SMOKE_TEST" == "true" ]] && log "SMOKE TEST MODE: reduced parameters"

    check_memtier
    check_redis_cli
    [[ "$SERVER_HOST" == "127.0.0.1" ]] && check_rust_binary

    # If --server-host is a remote host, skip starting local servers
    local local_mode=true
    [[ "$SERVER_HOST" != "127.0.0.1" ]] && local_mode=false

    # --- rust-redis throughput suite ---
    if [[ "$RUN_RUST" == "true" ]]; then
        [[ "$local_mode" == "true" ]] && start_rust_server "$PORT_RUST"
        run_throughput_suite "$PORT_RUST" "rust-redis"
        [[ "$local_mode" == "true" ]] && stop_rust_server
        # FLUSHALL between suite runs to reset key state
        redis-cli -p "$PORT_RUST" FLUSHALL &>/dev/null || true
    fi

    # --- Redis 7.x throughput suite ---
    if [[ "$RUN_REDIS" == "true" ]] && command -v redis-server &>/dev/null; then
        start_redis_server "$PORT_REDIS"
        run_throughput_suite "$PORT_REDIS" "redis-7x"
        stop_redis_server
    elif [[ "$RUN_REDIS" == "true" ]]; then
        log "WARNING: redis-server not found. Skipping Redis 7.x comparison."
        log "Install: brew install redis (macOS) | apt-get install redis-server (Linux)"
        RUN_REDIS=false
    fi

    # Memory, snapshot, open-loop sections -- implemented in Plan 02
    # Each section is called here and guarded by its flag:
    [[ "$RUN_MEMORY" == "true" ]] && run_memory_suite || true
    [[ "$RUN_SNAPSHOT" == "true" ]] && run_snapshot_suite || true
    [[ "$RUN_OPEN_LOOP" == "true" ]] && run_open_loop_suite || true

    # Generate BENCHMARK-v2.md -- implemented in Plan 03
    generate_report

    log "=== bench-v2.sh complete. Results: $OUTPUT_FILE ==="
}

main "$@"
```

**Stub functions for Plan 02 (prevents undefined function errors):**
```bash
run_memory_suite() { log "Memory suite: not yet implemented (Plan 02)"; }
run_snapshot_suite() { log "Snapshot suite: not yet implemented (Plan 02)"; }
run_open_loop_suite() { log "Open-loop suite: not yet implemented (Plan 02)"; }
generate_report() { log "Report generation: not yet implemented (Plan 03)"; }
```

After writing the file, make it executable:
```bash
chmod +x bench-v2.sh
```

Do NOT co-locate all functions in a single blob -- keep the section comments (Pre-population, Primary benchmark, etc.) so the file is navigable.
  </action>

  <acceptance_criteria>
    - bench-v2.sh is executable: `ls -la bench-v2.sh | grep -q "^-rwx"`
    - Script starts and prints usage on --help (or shows config on start): `bash bench-v2.sh --help 2>&1 | head -5` exits 0 or 1 with readable output
    - With memtier_benchmark missing, script prints install instructions and exits non-zero
    - Smoke test dry-run (memtier present): `bash bench-v2.sh --smoke-test --rust-only` completes without error on dev machine (may fail if server doesn't compile, but script logic is correct)
    - File contains the literal strings: `key-pattern=P:P`, `key-pattern=Z:Z`, `--rate-limiting`, `run_count`, `Z:Z` (Zipfian), `--hdr-file-prefix`
  </acceptance_criteria>

  <verify>
    <automated>chmod +x bench-v2.sh && bash -n bench-v2.sh && grep -c "key-pattern=Z:Z\|key-pattern=P:P\|rate-limiting\|hdr-file-prefix" bench-v2.sh</automated>
  </verify>

  <done>bench-v2.sh passes bash -n (syntax check), is executable, and contains the four required memtier methodology flags (Z:Z, P:P, rate-limiting, hdr-file-prefix)</done>
</task>

</tasks>

<verification>
1. `bash -n bench-v2.sh` returns exit 0 (no syntax errors)
2. `./bench-v2.sh 2>&1 | head -3` shows the log header or error about memtier not found (not a crash)
3. grep confirms: `key-pattern=P:P` and `key-pattern=Z:Z` are present (Pitfall 2 guard)
4. grep confirms: `--key-maximum` is present (Pitfall 1 guard)
5. `grep -c "run_count\|run-count" bench-v2.sh` returns >= 1 (5-iteration methodology)
</verification>

<success_criteria>
bench-v2.sh exists, passes syntax check, is executable, and encodes the required methodology (P:P pre-population, Z:Z benchmark, 5 iterations, HdrHistogram prefix, rate-limiting stubs). The --smoke-test flag reduces KEY_MAX to 1K and TEST_TIME to 10s for fast CI validation.
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-to-end-performance-validation-and-benchmark-suite/23-01-SUMMARY.md`
</output>
