---
phase: 36-cpu-and-memory-profiling
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - PROFILING-REPORT.md
autonomous: false
requirements:
  - PROF-04
  - PROF-05

must_haves:
  truths:
    - "PROFILING-REPORT.md contains per-key memory overhead data at multiple dataset sizes"
    - "PROFILING-REPORT.md contains CPU efficiency comparison across Tokio, Monoio, and Redis"
    - "PROFILING-REPORT.md contains fragmentation comparison"
    - "PROFILING-REPORT.md contains optimization recommendations based on measured data"
  artifacts:
    - path: "PROFILING-REPORT.md"
      provides: "Complete profiling report with tables and analysis"
      min_lines: 100
      contains: "Per-Key Memory Overhead"
  key_links:
    - from: "profile.sh"
      to: "PROFILING-REPORT.md"
      via: "script generates report"
      pattern: "OUTPUT_FILE"
---

<objective>
Execute the profiling suite (profile.sh) to generate PROFILING-REPORT.md with real measurement data comparing rust-redis (Tokio + Monoio) vs Redis 8.6.1 on memory and CPU.

Purpose: Produce the actionable profiling report that identifies optimization opportunities.
Output: PROFILING-REPORT.md with measured data
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-cpu-and-memory-profiling-vs-redis-flamegraph-hot-path-analysis-heap-profiling-per-key-overhead-measurement-tokio-vs-monoio-comparison/36-CONTEXT.md
@.planning/phases/36-cpu-and-memory-profiling-vs-redis-flamegraph-hot-path-analysis-heap-profiling-per-key-overhead-measurement-tokio-vs-monoio-comparison/36-01-SUMMARY.md
@profile.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build release binaries and execute profiling suite</name>
  <files>PROFILING-REPORT.md</files>
  <read_first>
    - profile.sh (understand flags, expected runtime, what it measures)
    - Cargo.toml (feature flags for tokio vs monoio builds)
  </read_first>
  <action>
1. Build release binaries:
   - `cargo build --release` (default tokio runtime)
   - Check if monoio feature builds: `cargo build --release --features runtime-monoio --no-default-features` — if it fails, use `--skip-monoio` flag

2. Ensure Redis is available: `which redis-server` and `redis-server --version`

3. Run the profiling script:
   - If monoio builds: `./profile.sh --output PROFILING-REPORT.md`
   - If monoio fails to build: `./profile.sh --output PROFILING-REPORT.md --skip-monoio`
   - Timeout: 10 minutes max (most measurements are fast, 1M key insertion takes longest)

4. If profile.sh fails at any point, debug the specific section:
   - Run failing section with `--section NAME` flag
   - Fix profile.sh if needed (minor bash issues)
   - Re-run

5. After profile.sh completes, review PROFILING-REPORT.md:
   - Verify all tables have data (no empty cells)
   - Verify per-key overhead numbers are reasonable (expect 50-500 bytes/key)
   - Verify CPU efficiency numbers are populated
   - Add a manual "Analysis and Recommendations" section at the bottom if the auto-generated one is insufficient

Handle edge cases:
- If redis-server not found: install via `brew install redis` or note in report
- If monoio build fails on macOS: skip monoio, note in report that monoio requires Linux io_uring
- If any measurement returns 0 or N/A: note it, don't let it crash the script
  </action>
  <verify>
    <automated>test -f PROFILING-REPORT.md && wc -l PROFILING-REPORT.md | awk '{print $1}' && grep -c "|" PROFILING-REPORT.md</automated>
  </verify>
  <acceptance_criteria>PROFILING-REPORT.md exists with 100+ lines. Contains markdown tables with actual measurement data. Per-key overhead section has data for at least 3 dataset sizes. CPU efficiency section has data for at least Redis and Tokio.</acceptance_criteria>
  <done>PROFILING-REPORT.md generated with real profiling data comparing rust-redis vs Redis on memory overhead, fragmentation, and CPU efficiency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review profiling report</name>
  <files>PROFILING-REPORT.md</files>
  <action>Present PROFILING-REPORT.md to user for review. The report contains per-key memory overhead, fragmentation ratios, CPU efficiency comparisons, and Criterion micro-benchmark results for rust-redis vs Redis 8.6.1.</action>
  <verify>
    <automated>test -f PROFILING-REPORT.md && echo "Report exists"</automated>
  </verify>
  <done>User has reviewed and approved the profiling report.</done>
  <what-built>Complete profiling suite: CompactKey benchmark (benches/compact_key.rs), automated profiling script (profile.sh), and profiling report (PROFILING-REPORT.md) comparing rust-redis vs Redis 8.6.1 on per-key memory overhead, memory fragmentation, CPU efficiency, and micro-benchmarks.</what-built>
  <how-to-verify>
    1. Review PROFILING-REPORT.md for data quality and completeness
    2. Check per-key memory overhead numbers — are they reasonable?
    3. Check CPU efficiency comparison — does it match your experience?
    4. Run `cargo bench --bench compact_key` to see CompactKey inline vs heap performance
    5. Optionally re-run specific sections: `./profile.sh --section per-key-memory`
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the measurements</resume-signal>
</task>

</tasks>

<verification>
- PROFILING-REPORT.md exists and has 100+ lines
- Contains per-key overhead data, CPU efficiency data, fragmentation data
- Criterion benchmarks (compact_key, entry_memory, get_hotpath) results included
- Report has hardware info header and date stamp
</verification>

<success_criteria>
1. PROFILING-REPORT.md contains real measurement data (not placeholders)
2. Memory overhead comparison shows per-key bytes for both servers at multiple scales
3. CPU efficiency section shows ops/sec per CPU% for each runtime
4. User has reviewed and approved the report quality
</success_criteria>

<output>
After completion, create `.planning/phases/36-cpu-and-memory-profiling-vs-redis-flamegraph-hot-path-analysis-heap-profiling-per-key-overhead-measurement-tokio-vs-monoio-comparison/36-02-SUMMARY.md`
</output>
