---
phase: 07-compare-architecture-resolve-bottlenecks-and-optimize-performance
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - BENCHMARK.md
autonomous: false
requirements: [OPT-04]

must_haves:
  truths:
    - "BENCHMARK.md contains before/after optimization comparison"
    - "Throughput at 50 clients pipeline 64 shows measurable improvement"
    - "Memory RSS with 100K keys shows reduction from Entry compaction"
  artifacts:
    - path: "BENCHMARK.md"
      provides: "Before/after optimization benchmark results"
      contains: "Optimization"
  key_links:
    - from: "bench.sh"
      to: "BENCHMARK.md"
      via: "benchmark runner produces data for report"
---

<objective>
Re-run benchmarks after all optimizations and produce before/after comparison in BENCHMARK.md.

Purpose: Validate that pipeline batching, Entry compaction, and parking_lot migration deliver measurable improvements.
Output: Updated BENCHMARK.md with optimization comparison section.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compare-architecture-resolve-bottlenecks-and-optimize-performance/07-CONTEXT.md
@.planning/phases/07-compare-architecture-resolve-bottlenecks-and-optimize-performance/07-01-SUMMARY.md
@.planning/phases/07-compare-architecture-resolve-bottlenecks-and-optimize-performance/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run post-optimization benchmarks and update BENCHMARK.md</name>
  <read_first>BENCHMARK.md, bench.sh</read_first>
  <files>BENCHMARK.md</files>
  <action>
1. Build the optimized release binary:
   ```bash
   cargo build --release
   ```

2. Run the benchmark suite (rust-redis only -- same parameters as Phase 6):
   ```bash
   ./bench.sh --rust-only --output BENCHMARK_AFTER.md
   ```
   This produces a fresh BENCHMARK_AFTER.md with post-optimization results.

3. Read both BENCHMARK.md (the "before" baseline from Phase 6) and BENCHMARK_AFTER.md (the "after" results).

4. Update BENCHMARK.md to include an "Optimization Results" section. Append (or replace the "Top 3 Bottlenecks" placeholder section) with:

```markdown
## Optimization Results (Phase 7)

### Changes Applied
1. **Pipeline batching** -- Pipelined commands collected into batches, executed under single lock acquisition
2. **Entry struct compaction** -- Removed created_at (16 bytes), version u64->u32 (4 bytes saved), last_access Instant->u32 (12 bytes saved) = ~32 bytes/key saved
3. **parking_lot::Mutex** -- Drop-in replacement for std::sync::Mutex (faster uncontended, no poisoning)
4. **Critical section minimization** -- Response writing and AOF logging moved outside lock hold

### Throughput Comparison (50 clients, pipeline 64, 3B data -- worst case scenario)

| Command | Before (ops/sec) | After (ops/sec) | Improvement |
|---------|-------------------|------------------|-------------|
| SET | {before} | {after} | {ratio}x |
| GET | {before} | {after} | {ratio}x |
| ... | ... | ... | ... |

### Memory Comparison (100K keys)

| Metric | Before | After | Improvement |
|--------|--------|-------|-------------|
| RSS at rest (KB) | {before} | {after} | ... |
| RSS with 100K keys (KB) | {before} | {after} | ... |
| Per-key overhead (estimated) | ~57 bytes metadata | ~25 bytes metadata | ~56% reduction |

### Key Observations

{Fill in based on actual benchmark data:}
- Throughput improvement at high concurrency (50 clients, pipeline 64)
- Memory reduction from Entry compaction
- CPU utilization change
- Any regressions at low concurrency (1 client, no pipeline)
```

5. Remove the BENCHMARK_AFTER.md temp file.

6. Ensure the original Phase 6 results remain in BENCHMARK.md (do not delete them). The new section is additive.
  </action>
  <verify>
    <automated>grep -q "Optimization Results" /Users/tindang/workspaces/tind-repo/rust-redis/BENCHMARK.md && grep -q "Pipeline batching" /Users/tindang/workspaces/tind-repo/rust-redis/BENCHMARK.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - BENCHMARK.md contains "Optimization Results" section
    - BENCHMARK.md contains before/after throughput comparison table
    - BENCHMARK.md contains memory comparison table
    - BENCHMARK.md retains the original Phase 6 results
  </acceptance_criteria>
  <done>BENCHMARK.md updated with before/after optimization comparison showing throughput and memory improvements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify optimization results</name>
  <files>BENCHMARK.md</files>
  <action>
Present the before/after benchmark results to the user for review. The full optimization suite includes:
- Entry compaction (32 bytes/key saved)
- Pipeline batching (single lock per pipeline batch instead of per-command)
- parking_lot::Mutex migration (faster uncontended lock)
- Critical section minimization (responses and AOF outside lock)
  </action>
  <verify>User confirms results are satisfactory</verify>
  <done>User has reviewed and approved the optimization results in BENCHMARK.md</done>
  <what-built>Full optimization suite: Entry compaction (32 bytes/key saved), pipeline batching (single lock per pipeline batch), parking_lot::Mutex migration. Benchmark results captured in BENCHMARK.md.</what-built>
  <how-to-verify>
    1. Review BENCHMARK.md -- check the "Optimization Results" section for throughput and memory improvements
    2. Key metrics to verify:
       - Throughput at 50 clients / pipeline 64 should show improvement (target: closer to Redis ratio)
       - Memory RSS with 100K keys should be lower than before
    3. Optionally re-run: `./bench.sh --rust-only` to independently verify
    4. Run `cargo test` to confirm all tests still pass
    5. Try `redis-cli -p 6379` against a running instance to verify basic functionality
  </how-to-verify>
  <resume-signal>Type "approved" if results look good, or describe any issues to address</resume-signal>
</task>

</tasks>

<verification>
1. `grep "Optimization Results" BENCHMARK.md` -- section exists
2. `cargo test` -- all tests pass
3. `cargo build --release` -- builds cleanly
</verification>

<success_criteria>
- BENCHMARK.md contains quantified before/after comparison
- Throughput improvement visible at high concurrency scenarios
- Memory reduction visible in RSS measurements
- All existing functionality preserved (full test suite passes)
- User has verified and approved the results
</success_criteria>

<output>
After completion, create `.planning/phases/07-compare-architecture-resolve-bottlenecks-and-optimize-performance/07-03-SUMMARY.md`
</output>
