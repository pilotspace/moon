---
phase: 06-setup-and-benchmark-real-use-cases
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - BENCHMARK.md
autonomous: false
requirements:
  - SETUP-03

user_setup:
  - service: redis
    why: "Baseline comparison server and redis-benchmark tool"
    env_vars: []
    dashboard_config:
      - task: "Install Redis via Homebrew"
        location: "Terminal: brew install redis"

must_haves:
  truths:
    - "BENCHMARK.md contains throughput numbers for all 13 commands across concurrency/pipeline/datasize combinations"
    - "BENCHMARK.md contains latency percentile data (p50, p99, p999) for each benchmark scenario"
    - "BENCHMARK.md contains CPU utilization observations during benchmark runs"
    - "BENCHMARK.md contains memory RSS measurements at rest and with 100K keys"
    - "BENCHMARK.md contains persistence overhead comparison (no AOF vs everysec vs always)"
    - "BENCHMARK.md contains side-by-side comparison with Redis 7.x if available"
    - "BENCHMARK.md identifies top 3 performance bottlenecks"
  artifacts:
    - path: "BENCHMARK.md"
      provides: "Complete benchmark results with analysis"
      contains: "ops/sec"
  key_links:
    - from: "bench.sh"
      to: "BENCHMARK.md"
      via: "Script generates markdown output"
      pattern: "BENCHMARK"
---

<objective>
Run the full benchmark suite and produce BENCHMARK.md with results and bottleneck analysis.

Purpose: Document actual performance characteristics of rust-redis with honest comparison against Redis 7.x.
Output: BENCHMARK.md with complete benchmark results
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-setup-and-benchmark-real-use-cases/06-CONTEXT.md
@.planning/phases/06-setup-and-benchmark-real-use-cases/06-RESEARCH.md
@.planning/phases/06-setup-and-benchmark-real-use-cases/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build release binary and run benchmark suite</name>
  <files>BENCHMARK.md</files>
  <read_first>
    - bench.sh (understand how to invoke and what flags it accepts)
    - .planning/phases/06-setup-and-benchmark-real-use-cases/06-CONTEXT.md (expected metrics and format)
    - .planning/phases/06-setup-and-benchmark-real-use-cases/06-RESEARCH.md (pitfalls: port conflicts, AOF file sizes, MSET format)
  </read_first>
  <action>
**Pre-flight checks:**
1. Run `cargo build --release` to ensure the release binary is ready
2. Check if `redis-benchmark` is installed: `which redis-benchmark`. If not installed, prompt user to run `brew install redis`
3. Check if `redis-server` is installed for baseline comparison
4. Verify our server handles the MSET format redis-benchmark uses: start rust-redis on port 6399, run `redis-benchmark -p 6399 -t mset -n 10 -q -e` and check for errors. Kill server after test.

**Run benchmarks:**
5. Execute `./bench.sh --output BENCHMARK.md` (or `./bench.sh --rust-only --output BENCHMARK.md` if redis-server is unavailable)
6. This will take several minutes. The script manages server lifecycle.

**Post-processing:**
7. Review the generated BENCHMARK.md for completeness
8. Verify BENCHMARK.md contains latency percentile data (p50, p99, p99.9 columns) in the throughput tables. If the bench.sh output is missing these columns, manually parse the CSV result files in the temp dir (or re-run with fixes) to ensure percentile data is present.
9. Verify BENCHMARK.md contains a CPU utilization section with average CPU % per benchmark phase. If missing, add a section titled "## CPU Utilization" with observations from the cpu_*.txt files or from running `ps -o %cpu` during a quick benchmark re-run.
10. Add a "Top 3 Bottlenecks" analysis section at the end based on the data:
   - Look for commands where rust-redis is significantly slower than Redis 7.x
   - Look for cases where pipelining improvement is sublinear
   - Look for high latency tail (p999) outliers -- use the latency percentile data to identify commands with large p99/p999 spread
   - Note any correlation between high CPU utilization and throughput drops
   - Write 2-3 sentences per bottleneck explaining likely cause and future optimization direction
11. Add a "Test Environment" section at the top documenting: OS version, CPU model/cores, RAM, Rust version, Redis version

If redis-benchmark is not available and cannot be installed, create BENCHMARK.md with the benchmark script instructions and placeholder tables marked "Run `./bench.sh` to populate results."
  </action>
  <verify>
    <automated>test -f BENCHMARK.md && grep -q "ops/sec" BENCHMARK.md && grep -qi "p50\|p99\|latency" BENCHMARK.md && grep -qi "cpu" BENCHMARK.md && echo "BENCHMARK.md exists with results, latency percentiles, and CPU data"</automated>
  </verify>
  <acceptance_criteria>
    - BENCHMARK.md exists at project root
    - BENCHMARK.md contains "ops/sec" (actual throughput numbers or placeholder)
    - BENCHMARK.md contains "SET" and "GET" command results
    - BENCHMARK.md contains latency percentile data (p50, p99, p99.9)
    - BENCHMARK.md contains CPU utilization section with observations
    - BENCHMARK.md contains memory section with RSS measurements
    - BENCHMARK.md contains persistence overhead section
    - BENCHMARK.md contains "Bottleneck" or "Analysis" section
    - BENCHMARK.md contains test environment information
  </acceptance_criteria>
  <done>BENCHMARK.md contains complete benchmark results across all specified workload scenarios, latency percentiles (p50/p99/p999), CPU utilization observations, memory measurements, persistence overhead, and bottleneck analysis.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify benchmark results and project packaging</name>
  <files>BENCHMARK.md, README.md, Dockerfile, .github/workflows/ci.yml</files>
  <action>Present all Phase 6 deliverables for user review. No automated changes -- this is a verification checkpoint.</action>
  <what-built>Complete project packaging (README, Dockerfile, CI, benchmarks) for rust-redis</what-built>
  <how-to-verify>
    1. Review README.md -- does it accurately describe the project, list all features, and show correct CLI flags?
    2. Review BENCHMARK.md -- are the numbers reasonable? Are the comparison tables clear? Do they include latency percentiles (p50/p99/p999) and CPU utilization data?
    3. Test Docker build: `docker build -t rust-redis .` (should succeed and produce a small image)
    4. Test Docker run: `docker run -p 6380:6379 rust-redis` then `redis-cli -p 6380 PING` (should get PONG)
    5. Review .github/workflows/ci.yml -- does it look correct for your repository?
    6. Check `cargo test` still passes (no regressions)
  </how-to-verify>
  <verify>
    <automated>test -f BENCHMARK.md && test -f README.md && test -f Dockerfile && test -f .github/workflows/ci.yml && echo "All deliverables present"</automated>
  </verify>
  <done>User has reviewed and approved all Phase 6 deliverables: README, BENCHMARK.md, Dockerfile, CI config.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- BENCHMARK.md has populated results tables with throughput and latency percentiles
- BENCHMARK.md contains CPU utilization observations
- README.md, Dockerfile, CI config all present and correct
- Docker build succeeds
- cargo test passes
</verification>

<success_criteria>
- BENCHMARK.md documents actual performance with honest numbers including latency percentiles and CPU utilization
- Side-by-side comparison with Redis 7.x (if available)
- Top 3 bottlenecks identified with analysis
- All Phase 6 deliverables complete and verified
</success_criteria>

<output>
After completion, create `.planning/phases/06-setup-and-benchmark-real-use-cases/06-03-SUMMARY.md`
</output>
