---
phase: 21-lua-scripting-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/shard/dispatch.rs
  - src/shard/mod.rs
  - src/server/connection.rs
  - tests/integration.rs
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - SCRIPT-01
  - SCRIPT-02
  - SCRIPT-03
  - SANDBOX-01
  - SANDBOX-02
  - REDIS-01
  - REDIS-02
  - TIMEOUT-01
  - CROSSSLOT-01

must_haves:
  truths:
    - "EVAL command executes a Lua script accessing KEYS[1] and ARGV[1] and returns the correct frame"
    - "EVALSHA returns the same result as EVAL for a previously loaded script"
    - "EVALSHA returns NOSCRIPT error for an unknown SHA1"
    - "SCRIPT LOAD returns the SHA1 and fans out the script to all shards"
    - "SCRIPT EXISTS returns 1 for a loaded script SHA1 and 0 for an unknown SHA1"
    - "SCRIPT FLUSH clears the cache (SCRIPT EXISTS returns 0 after flush)"
    - "redis.call inside a Lua script executes a real Redis command (SET/GET round-trip)"
    - "redis.pcall catches errors without propagating as Lua errors"
    - "Sandbox blocks os.clock() as the only allowed os function; load/dofile/require error"
    - "A script exceeding 5 seconds returns BUSY timeout error"
    - "Keys on different shards in EVAL return CROSSSLOT error when num_shards > 1"
  artifacts:
    - path: "src/shard/dispatch.rs"
      provides: "ShardMessage::ScriptLoad variant for cross-shard script cache fan-out"
      contains: "ScriptLoad"
    - path: "src/shard/mod.rs"
      provides: "Lua VM and ScriptCache initialized in Shard::run(), ScriptLoad message handled in SPSC drain"
      contains: "setup_lua_vm"
    - path: "src/server/connection.rs"
      provides: "EVAL/EVALSHA/SCRIPT intercept in handle_connection_sharded before normal dispatch"
      contains: "handle_eval"
    - path: "tests/integration.rs"
      provides: "Integration tests for all scripting requirements"
      contains: "test_eval_basic"
  key_links:
    - from: "src/server/connection.rs"
      to: "src/scripting/mod.rs"
      via: "handle_eval/handle_evalsha/handle_script_subcommand called before normal dispatch loop"
      pattern: "scripting::handle_eval"
    - from: "src/shard/mod.rs"
      to: "src/scripting/mod.rs"
      via: "setup_lua_vm() called in Shard::run() after databases Rc<RefCell> wrapping"
      pattern: "setup_lua_vm"
    - from: "src/server/connection.rs"
      to: "src/shard/dispatch.rs"
      via: "ShardMessage::ScriptLoad sent via dispatch_tx for SCRIPT LOAD fan-out"
      pattern: "ScriptLoad"
    - from: "src/shard/mod.rs"
      to: "src/shard/dispatch.rs"
      via: "ShardMessage::ScriptLoad handled in SPSC drain loop: inserts script into local cache"
      pattern: "ScriptLoad"
---

<objective>
Wire the scripting module into the shard and connection infrastructure:
1. Add ShardMessage::ScriptLoad to the SPSC dispatch enum
2. Initialize Lua VM and ScriptCache in Shard::run() after databases are wrapped in Rc<RefCell>
3. Handle ScriptLoad messages in the SPSC drain loop (cross-shard cache fan-out)
4. Intercept EVAL/EVALSHA/SCRIPT in handle_connection_sharded before the normal dispatch loop
5. Add integration tests covering all scripting requirements

Purpose: Connect the self-contained scripting module (Plan 01) to the running server.
Output: Working EVAL/EVALSHA/SCRIPT commands on a live server, verified by integration tests.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-lua-scripting-engine/21-CONTEXT.md
@.planning/phases/21-lua-scripting-engine/21-RESEARCH.md
@.planning/phases/21-lua-scripting-engine/21-01-SUMMARY.md
@.planning/STATE.md

<interfaces>
From src/shard/dispatch.rs (add ScriptLoad variant):
  pub enum ShardMessage {
      NewConnection(TcpStream),
      Execute { db_index, command, reply_tx },
      MultiExecute { db_index, commands, reply_tx },
      PubSubFanOut { channel, message },
      SnapshotBegin { epoch, snapshot_dir, reply_tx },
      BlockRegister { ... },
      BlockCancel { wait_id },
      RegisterReplica { replica_id, tx },
      UnregisterReplica { replica_id },
      GetKeysInSlot { ... },
      SlotOwnershipUpdate { ... },
      Shutdown,
      // ADD: ScriptLoad { sha1: String, script: Bytes }
  }

From src/shard/mod.rs (Shard::run signature):
  pub async fn run(
      &mut self,
      mut conn_rx: mpsc::Receiver<tokio::net::TcpStream>,
      mut consumers: Vec<HeapCons<ShardMessage>>,
      producers: Vec<HeapProd<ShardMessage>>,
      shutdown: CancellationToken,
      aof_tx: Option<mpsc::Sender<AofMessage>>,
      bind_addr: Option<String>,
      persistence_dir: Option<String>,
      mut snapshot_trigger_rx: tokio::sync::watch::Receiver<u64>,
      repl_state_ext: Option<Arc<RwLock<ReplicationState>>>,
      cluster_state: Option<Arc<RwLock<ClusterState>>>,
      config_port: u16,
  )
  -- After databases/dispatch_tx/pubsub_rc wrapping, add:
  let lua_rc = crate::scripting::setup_lua_vm().expect("Lua VM init");
  let script_cache_rc = Rc::new(RefCell::new(crate::scripting::ScriptCache::new()));
  -- Clone both into handle_connection_sharded spawns
  -- Handle ScriptLoad in SPSC drain alongside PubSubFanOut

From src/server/connection.rs (handle_connection_sharded signature):
  pub async fn handle_connection_sharded(
      stream: TcpStream,
      databases: Rc<RefCell<Vec<Database>>>,
      shard_id: usize,
      num_shards: usize,
      dispatch_tx: Rc<RefCell<Vec<HeapProd<ShardMessage>>>>,
      pubsub_registry: Rc<RefCell<PubSubRegistry>>,
      blocking_registry: Rc<RefCell<BlockingRegistry>>,
      shutdown: CancellationToken,
      requirepass: Option<String>,
      aof_tx: Option<mpsc::Sender<AofMessage>>,
      tracking_table: Rc<RefCell<TrackingTable>>,
      client_id: u64,
      repl_state: Option<Arc<RwLock<ReplicationState>>>,
      cluster_state: Option<Arc<RwLock<ClusterState>>>,
      config_port: u16,
  )
  -- Add two new parameters AFTER cluster_state, BEFORE config_port:
      lua: Rc<mlua::Lua>,
      script_cache: Rc<RefCell<crate::scripting::ScriptCache>>,
  -- Intercept location: after CLUSTER block, before MULTI/EXEC handling

From src/scripting/mod.rs (Plan 01 outputs):
  pub fn setup_lua_vm() -> mlua::Result<Rc<mlua::Lua>>
  pub fn handle_eval(lua, cache, args, db, shard_id, num_shards, selected_db, db_count) -> Frame
  pub fn handle_evalsha(lua, cache, args, db, shard_id, num_shards, selected_db, db_count) -> Frame
  pub fn handle_script_subcommand(cache, args) -> (Frame, Option<(String, Bytes)>)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ScriptLoad shard message, wire VM into Shard::run, and intercept EVAL in connection handler</name>
  <files>src/shard/dispatch.rs, src/shard/mod.rs, src/server/connection.rs</files>
  <read_first>
    - src/shard/dispatch.rs (full file ~100 lines) -- add ScriptLoad variant to ShardMessage enum
    - src/shard/mod.rs (lines 1-260) -- Shard::run() setup, Rc<RefCell> wrapping, spawn_local calls
    - src/shard/mod.rs (lines 260-400) -- SPSC consumer drain loop where PubSubFanOut is handled
    - src/server/connection.rs (lines 1236-1280) -- handle_connection_sharded function signature
    - src/server/connection.rs (lines 1360-1440) -- CLUSTER intercept block (intercept EVAL after this block)
    - src/server/connection.rs (lines 1440-1520) -- where MULTI/EXEC intercept begins (EVAL intercept goes before this)
  </read_first>
  <action>
STEP 1 -- src/shard/dispatch.rs: Add ScriptLoad variant to ShardMessage enum.
Add BEFORE the Shutdown variant:
    /// Fan-out a loaded script to all shards so EVALSHA works regardless of which shard receives it.
    /// Sent by the connection handler on SCRIPT LOAD; received by all other shards' SPSC drain loops.
    ScriptLoad {
        sha1: String,
        script: bytes::Bytes,
    },

STEP 2 -- src/shard/mod.rs: Initialize Lua VM and ScriptCache in Shard::run().
Location: After the line `let blocking_rc = Rc::new(RefCell::new(BlockingRegistry::new(shard_id)));`
and BEFORE the snapshot_state declaration.
Insert:
    // Initialize per-shard Lua VM (created inside run() because Lua is !Send -- Pitfall 1)
    let lua_rc: Rc<mlua::Lua> = crate::scripting::setup_lua_vm()
        .expect("Lua VM initialization failed");
    let script_cache_rc = Rc::new(RefCell::new(crate::scripting::ScriptCache::new()));

Add imports at top of shard/mod.rs:
    use std::rc::Rc;  (already present)
    use mlua;  -- NOT needed, use fully qualified paths

STEP 3 -- src/shard/mod.rs: Handle ShardMessage::ScriptLoad in the SPSC drain loop.
Find the match arm for ShardMessage::PubSubFanOut. Add immediately after it:
    ShardMessage::ScriptLoad { sha1, script } => {
        // Fan-out: cache this script on this shard so EVALSHA works locally
        let mut cache = script_cache_rc.borrow_mut();
        // Verify SHA1 matches before inserting (defensive check)
        let computed = sha1_smol::Sha1::from(&script[..]).hexdigest();
        if computed == sha1 {
            cache.load(script);
        }
    }

STEP 4 -- src/shard/mod.rs: Clone lua_rc and script_cache_rc into each spawn_local call.
Find all `tokio::task::spawn_local(handle_connection_sharded(` calls (there should be one for Tokio path
and possibly one for io_uring fallback). Add two parameters after `cluster_state.clone()`:
    lua_rc.clone(),
    script_cache_rc.clone(),

STEP 5 -- src/server/connection.rs: Add lua and script_cache parameters to handle_connection_sharded.
Current last parameters are:
    cluster_state: Option<Arc<RwLock<crate::cluster::ClusterState>>>,
    config_port: u16,
Add BEFORE config_port (to avoid shifting config_port to ambiguous position):
    lua: std::rc::Rc<mlua::Lua>,
    script_cache: std::rc::Rc<std::cell::RefCell<crate::scripting::ScriptCache>>,
Add import at top of connection.rs: use mlua; -- NOT needed, use fully qualified paths.

STEP 6 -- src/server/connection.rs: Intercept EVAL/EVALSHA/SCRIPT commands.
Location: After the CLUSTER block (after the `continue;` that ends the CLUSTER arm, around line 1381),
BEFORE the cluster slot routing block (the `if crate::cluster::cluster_enabled()` block).

Insert:
    // --- EVAL / EVALSHA: Lua script execution ---
    if cmd.eq_ignore_ascii_case(b"EVAL") || cmd.eq_ignore_ascii_case(b"EVALSHA") {
        let response = {
            let mut dbs = databases.borrow_mut();
            let db = &mut dbs[selected_db];
            let db_count = dbs.len();
            if cmd.eq_ignore_ascii_case(b"EVAL") {
                crate::scripting::handle_eval(
                    &lua,
                    &script_cache,
                    cmd_args,
                    db,
                    shard_id,
                    num_shards,
                    selected_db,
                    db_count,
                )
            } else {
                crate::scripting::handle_evalsha(
                    &lua,
                    &script_cache,
                    cmd_args,
                    db,
                    shard_id,
                    num_shards,
                    selected_db,
                    db_count,
                )
            }
        };
        responses.push(response);
        continue;
    }

    // --- SCRIPT subcommands: LOAD, EXISTS, FLUSH ---
    if cmd.eq_ignore_ascii_case(b"SCRIPT") {
        let (response, fanout) = crate::scripting::handle_script_subcommand(&script_cache, cmd_args);
        // SCRIPT LOAD: fan-out the script to all OTHER shards via SPSC mesh
        if let Some((sha1, script)) = fanout {
            let txs = dispatch_tx.borrow();
            for (idx, tx) in txs.iter().enumerate() {
                if idx != shard_id {
                    use ringbuf::traits::Producer;
                    let msg = crate::shard::dispatch::ShardMessage::ScriptLoad {
                        sha1: sha1.clone(),
                        script: script.clone(),
                    };
                    // try_push: non-blocking; SPSC ring buffer; drop if full (rare edge case)
                    let _ = unsafe { &*(tx as *const _) as &ringbuf::HeapProd<_> }.try_push(msg);
                }
            }
        }
        responses.push(response);
        continue;
    }

NOTE on the try_push call: The dispatch_tx Vec<HeapProd<ShardMessage>> borrows from Rc<RefCell<Vec<HeapProd<ShardMessage>>>>.
Look at how PubSubFanOut uses dispatch_tx elsewhere in this file for the correct pattern.
If the existing pattern uses a different method (e.g., push_overwrite or a helper), use that same pattern.
The key invariant: SCRIPT LOAD response returns immediately; fan-out to other shards is best-effort non-blocking.
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep "^error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - cargo build succeeds with zero errors
    - ShardMessage::ScriptLoad { sha1: String, script: Bytes } variant exists in dispatch.rs
    - src/shard/mod.rs calls setup_lua_vm() and creates script_cache_rc inside run()
    - ScriptLoad message handled in SPSC drain loop alongside PubSubFanOut
    - handle_connection_sharded signature accepts lua: Rc<mlua::Lua> and script_cache parameters
    - EVAL/EVALSHA block calls crate::scripting::handle_eval / handle_evalsha before cluster routing
    - SCRIPT block calls crate::scripting::handle_script_subcommand and fans out ScriptLoad to other shards
  </acceptance_criteria>
  <done>Server compiles. EVAL/EVALSHA/SCRIPT are wired into the sharded connection handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for all scripting requirements</name>
  <files>tests/integration.rs</files>
  <read_first>
    - tests/integration.rs (last 100 lines) -- find existing test helpers (start_test_server, redis_client, etc.) and the end of file to append new tests
    - .planning/phases/21-lua-scripting-engine/21-RESEARCH.md (Validation Architecture section, lines ~604-638) -- test names and requirement IDs
  </read_first>
  <behavior>
    - test_eval_basic: EVAL "return KEYS[1]" 1 mykey -> BulkString("mykey")
    - test_eval_argv: EVAL "return ARGV[1]" 0 hello -> BulkString("hello")
    - test_evalsha: SCRIPT LOAD returns SHA1; EVALSHA with that SHA1 returns same result as EVAL
    - test_evalsha_noscript: EVALSHA with unknown sha1 hex returns error starting with "NOSCRIPT"
    - test_script_load: SCRIPT LOAD returns 40-char lowercase hex SHA1
    - test_script_exists: SCRIPT EXISTS returns [1] for loaded SHA1, [0] for unknown SHA1
    - test_script_flush: after SCRIPT FLUSH, SCRIPT EXISTS returns [0]; EVALSHA returns NOSCRIPT
    - test_sandbox_restrictions: EVAL "return load('return 1')()" 0 -> error; EVAL "return dofile('/tmp/x')" 0 -> error
    - test_sandbox_allowed: EVAL "return string.upper('hello')" 0 -> BulkString("HELLO")
    - test_redis_call: EVAL "redis.call('SET', KEYS[1], ARGV[1]); return redis.call('GET', KEYS[1])" 1 mykey myval -> BulkString("myval")
    - test_redis_pcall: EVAL "local r = redis.pcall('INVALID'); return r.err" 0 -> non-nil string (the error message)
    - test_script_timeout: EVAL "while true do end" 0 -> error containing "BUSY" or "timeout" within reasonable time
    - test_eval_return_types: integer, string, table, nil/false return correct RESP types
  </behavior>
  <action>
Read the end of tests/integration.rs to find the last test function and module structure.
Then append a new test section after all existing tests:

// ============================================================
// Phase 21: Lua Scripting Engine
// ============================================================

Each test uses the existing test helpers from the file (start_test_server or equivalent).
Use the `redis` crate client (already a dev-dependency) for sending commands.

Structure each test as:
  #[tokio::test]
  async fn test_eval_basic() {
      let (_guard, addr) = start_test_server().await;
      let client = redis::Client::open(format!("redis://{addr}")).unwrap();
      let mut con = client.get_multiplexed_tokio_connection().await.unwrap();
      let result: String = redis::cmd("EVAL")
          .arg("return KEYS[1]")
          .arg(1)
          .arg("mykey")
          .query_async(&mut con).await.unwrap();
      assert_eq!(result, "mykey");
  }

For test_eval_argv:
  let result: String = redis::cmd("EVAL")
      .arg("return ARGV[1]").arg(0).arg("hello")
      .query_async(&mut con).await.unwrap();
  assert_eq!(result, "hello");

For test_evalsha (SCRIPT LOAD then EVALSHA):
  let sha: String = redis::cmd("SCRIPT").arg("LOAD").arg("return KEYS[1]")
      .query_async(&mut con).await.unwrap();
  assert_eq!(sha.len(), 40); // 40-char hex SHA1
  let result: String = redis::cmd("EVALSHA")
      .arg(&sha).arg(1).arg("testkey")
      .query_async(&mut con).await.unwrap();
  assert_eq!(result, "testkey");

For test_evalsha_noscript:
  let result = redis::cmd("EVALSHA")
      .arg("0000000000000000000000000000000000000000")
      .arg(0)
      .query_async::<String>(&mut con).await;
  assert!(result.is_err());
  let err_msg = result.unwrap_err().to_string();
  assert!(err_msg.contains("NOSCRIPT"), "expected NOSCRIPT, got: {err_msg}");

For test_script_load:
  let sha: String = redis::cmd("SCRIPT").arg("LOAD").arg("return 42")
      .query_async(&mut con).await.unwrap();
  assert_eq!(sha.len(), 40);
  assert!(sha.chars().all(|c| c.is_ascii_hexdigit()));

For test_script_exists:
  let sha: String = redis::cmd("SCRIPT").arg("LOAD").arg("return 1")
      .query_async(&mut con).await.unwrap();
  let results: Vec<i64> = redis::cmd("SCRIPT")
      .arg("EXISTS").arg(&sha).arg("0000000000000000000000000000000000000000")
      .query_async(&mut con).await.unwrap();
  assert_eq!(results, vec![1, 0]);

For test_script_flush:
  let sha: String = redis::cmd("SCRIPT").arg("LOAD").arg("return 1")
      .query_async(&mut con).await.unwrap();
  let _: String = redis::cmd("SCRIPT").arg("FLUSH").query_async(&mut con).await.unwrap();
  let results: Vec<i64> = redis::cmd("SCRIPT").arg("EXISTS").arg(&sha)
      .query_async(&mut con).await.unwrap();
  assert_eq!(results, vec![0]);

For test_sandbox_restrictions (load and dofile should error):
  let result = redis::cmd("EVAL")
      .arg("return load('return 1')()")
      .arg(0)
      .query_async::<redis::Value>(&mut con).await;
  assert!(result.is_err(), "load() should be sandboxed");

For test_sandbox_allowed (string.* available):
  let result: String = redis::cmd("EVAL")
      .arg("return string.upper('hello')").arg(0)
      .query_async(&mut con).await.unwrap();
  assert_eq!(result, "HELLO");

For test_redis_call (SET then GET inside script):
  let result: String = redis::cmd("EVAL")
      .arg("redis.call('SET', KEYS[1], ARGV[1]); return redis.call('GET', KEYS[1])")
      .arg(1).arg("lua_test_key").arg("lua_test_value")
      .query_async(&mut con).await.unwrap();
  assert_eq!(result, "lua_test_value");
  // Verify the key was actually set in the database
  let direct: String = redis::cmd("GET").arg("lua_test_key")
      .query_async(&mut con).await.unwrap();
  assert_eq!(direct, "lua_test_value");

For test_redis_pcall (error caught, not propagated):
  // Call an invalid command -- pcall catches it as {err=...} table
  let result: String = redis::cmd("EVAL")
      .arg("local r = redis.pcall('NOTACOMMAND'); if r.err then return 'caught' else return 'uncaught' end")
      .arg(0)
      .query_async(&mut con).await.unwrap();
  assert_eq!(result, "caught");

For test_script_timeout (infinite loop returns BUSY):
  // Use a shorter timeout for test speed; the default is 5s but integration test needs to be fast.
  // The script itself: while true do end -- guaranteed infinite
  let result = redis::cmd("EVAL")
      .arg("local i = 0; while true do i = i + 1 end")
      .arg(0)
      .query_async::<redis::Value>(&mut con).await;
  // Should be an error (timeout fires via hook)
  assert!(result.is_err());
  let err_msg = result.unwrap_err().to_string();
  assert!(
      err_msg.contains("BUSY") || err_msg.contains("timeout") || err_msg.contains("script"),
      "expected timeout error, got: {err_msg}"
  );
  // NOTE: This test may take up to 5 seconds. That is acceptable for a phase gate test.

For test_eval_return_types:
  // Integer return
  let n: i64 = redis::cmd("EVAL").arg("return 42").arg(0)
      .query_async(&mut con).await.unwrap();
  assert_eq!(n, 42);
  // Float truncated to integer
  let f: i64 = redis::cmd("EVAL").arg("return 3.99").arg(0)
      .query_async(&mut con).await.unwrap();
  assert_eq!(f, 3);
  // Nil returns as null/nil (redis crate returns Value::Nil)
  let nil_result: redis::Value = redis::cmd("EVAL").arg("return nil").arg(0)
      .query_async(&mut con).await.unwrap();
  assert!(matches!(nil_result, redis::Value::Nil));
  // Table returns as array
  let arr: Vec<i64> = redis::cmd("EVAL").arg("return {1, 2, 3}").arg(0)
      .query_async(&mut con).await.unwrap();
  assert_eq!(arr, vec![1, 2, 3]);
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/rust-redis && cargo test scripting 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All test_eval_*, test_evalsha_*, test_script_*, test_sandbox_*, test_redis_*, test_script_timeout tests compile
    - cargo test scripting passes (all tests green)
    - test_eval_basic: KEYS[1] round-trip works
    - test_evalsha: SHA1 length is 40 characters, EVALSHA returns correct result
    - test_evalsha_noscript: error contains "NOSCRIPT"
    - test_script_flush: SCRIPT EXISTS returns 0 after flush
    - test_sandbox_restrictions: load() is blocked
    - test_sandbox_allowed: string.upper works
    - test_redis_call: GET after SET inside Lua returns correct value, key visible from outside script too
    - test_redis_pcall: error is caught and returned as table, not propagated
    - test_script_timeout: infinite loop returns error containing BUSY/timeout/script
    - test_eval_return_types: integer, float truncation, nil, array all correct
  </acceptance_criteria>
  <done>All scripting integration tests pass. cargo test scripting exits with zero failures.</done>
</task>

</tasks>

<verification>
cargo build 2>&1 | grep -c "^error"  -- must output 0
cargo test scripting -- --nocapture 2>&1 | grep -E "FAILED|ok"  -- all "ok", zero "FAILED"
grep "ScriptLoad" src/shard/dispatch.rs  -- must find the new variant
grep "handle_eval" src/server/connection.rs  -- must find the intercept call
grep "setup_lua_vm" src/shard/mod.rs  -- must find the VM init call
</verification>

<success_criteria>
- Server compiles and starts successfully
- EVAL/EVALSHA/SCRIPT commands are intercepted before the normal dispatch loop
- Scripts execute atomically within a shard (enforced by single-threaded shard model)
- SCRIPT LOAD fans out to all shards via ShardMessage::ScriptLoad
- All integration tests pass: EVAL-01 through CROSSSLOT-01 requirements verified
- Script timeout terminates with BUSY error (not a panic or hang)
- Sandbox prevents load/dofile while allowing string.upper/math.* etc.
- redis.call executes real commands; mutations persist after script completes
</success_criteria>

<output>
After completion, create .planning/phases/21-lua-scripting-engine/21-02-SUMMARY.md with:
- All files modified
- Key wiring decisions (parameter ordering, intercept location in connection loop)
- Integration test results (pass/fail counts)
- Any deviations from RESEARCH.md patterns
</output>
