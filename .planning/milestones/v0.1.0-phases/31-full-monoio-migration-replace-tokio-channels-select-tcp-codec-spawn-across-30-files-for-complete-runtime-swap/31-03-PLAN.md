---
phase: 31-full-monoio-migration
plan: 03
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/persistence/aof.rs
  - src/persistence/auto_save.rs
  - src/persistence/wal.rs
  - src/replication/master.rs
  - src/replication/replica.rs
  - src/replication/state.rs
  - src/cluster/bus.rs
  - src/cluster/gossip.rs
  - src/cluster/failover.rs
  - src/blocking/mod.rs
  - src/pubsub/subscriber.rs
  - src/tracking/mod.rs
  - src/acl/io.rs
autonomous: true
requirements: [MONOIO-FULL-03]

must_haves:
  truths:
    - "Persistence files (aof, auto_save, wal) use runtime-agnostic channels and cancellation"
    - "Replication files use cfg-gated TCP and runtime-agnostic channels"
    - "Cluster files use cfg-gated TCP and runtime-agnostic channels/cancellation"
    - "Blocking module uses runtime-agnostic oneshot channels"
    - "PubSub subscriber uses runtime-agnostic mpsc channels"
    - "Tracking module uses runtime-agnostic mpsc channels"
    - "ACL I/O uses cfg-gated or std filesystem operations"
    - "All existing tests pass under runtime-tokio"
  artifacts:
    - path: "src/persistence/aof.rs"
      provides: "AOF writer with runtime-agnostic channels and cancellation"
    - path: "src/cluster/bus.rs"
      provides: "Cluster bus with cfg-gated TCP and runtime-agnostic cancellation"
    - path: "src/blocking/mod.rs"
      provides: "BlockingRegistry with runtime-agnostic oneshot channels"
  key_links:
    - from: "src/persistence/aof.rs"
      to: "src/runtime/channel.rs"
      via: "MpscReceiver for AOF messages"
      pattern: "channel::MpscReceiver"
    - from: "src/replication/master.rs"
      to: "src/runtime/channel.rs"
      via: "OneshotSender for snapshot completion"
      pattern: "channel::oneshot"
    - from: "src/cluster/bus.rs"
      to: "src/runtime/cancel.rs"
      via: "CancellationToken for shutdown"
      pattern: "cancel::CancellationToken"
---

<objective>
Migrate remaining subsystems (persistence, replication, cluster, blocking, pubsub, tracking, acl) from direct Tokio types to runtime-agnostic primitives.

Purpose: Eliminates the remaining ~61 compile errors across 13 files. After this plan + Plan 02, all source files will be free of unconditional tokio dependencies.
Output: All 26 affected source files migrated.
</objective>

<execution_context>
@/Users/tindang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tindang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-full-monoio-migration-replace-tokio-channels-select-tcp-codec-spawn-across-30-files-for-complete-runtime-swap/31-01-SUMMARY.md
@src/runtime/channel.rs
@src/runtime/cancel.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate persistence and replication subsystems</name>
  <files>src/persistence/aof.rs, src/persistence/auto_save.rs, src/persistence/wal.rs, src/replication/master.rs, src/replication/replica.rs, src/replication/state.rs</files>
  <read_first>
    - src/persistence/aof.rs lines 1-20 (imports)
    - src/persistence/aof.rs lines 140-180 (AofWriter struct with CancellationToken, tokio::select!)
    - src/persistence/auto_save.rs (full file -- small, uses CancellationToken, watch, select!)
    - src/persistence/wal.rs lines 1-20 (imports -- check what tokio types it uses)
    - src/replication/master.rs lines 1-20 (imports)
    - src/replication/master.rs lines 60-90 (oneshot usage for snapshots)
    - src/replication/replica.rs lines 1-20 (imports: TcpStream, AsyncRead/Write, sleep)
    - src/replication/state.rs lines 1-20 (imports)
    - src/runtime/channel.rs (Plan 01 output)
    - src/runtime/cancel.rs (Plan 01 output)
  </read_first>
  <action>
**persistence/aof.rs (10 errors):**
- Replace `use tokio::sync::mpsc;` with `use crate::runtime::channel;`
- Replace `use tokio_util::sync::CancellationToken;` with `use crate::runtime::cancel::CancellationToken;`
- Update `MpscReceiver` type in AofWriter struct
- Replace `tokio::select!` with cfg-gated select (there is one select! in the event loop)
- Replace `.recv().await` with `.recv_async().await` on flume receivers
- The `tokio::io::AsyncWriteExt` import in the test module needs `#[cfg(feature = "runtime-tokio")]` gating

**persistence/auto_save.rs (9 errors):**
- Replace `use tokio_util::sync::CancellationToken;` with `use crate::runtime::cancel::CancellationToken;`
- Replace `tokio::sync::watch::Sender/Receiver` with `crate::runtime::channel::WatchSender/WatchReceiver`
- Replace `tokio::select!` with cfg-gated select (there are 2-3 select! sites)
- Update timer usage to use `crate::runtime::traits::RuntimeTimer` sleep/interval

**persistence/wal.rs (2 errors):**
- Check what tokio types are used and replace. Likely `tokio::sync::mpsc` or similar.

**replication/master.rs (11 errors):**
- Replace `tokio::io::AsyncWriteExt` with cfg-gated import
- Replace `tokio::net::tcp::OwnedWriteHalf` with cfg-gated import:
  ```rust
  #[cfg(feature = "runtime-tokio")]
  use tokio::net::tcp::OwnedWriteHalf;
  #[cfg(feature = "runtime-monoio")]
  // monoio TcpStream doesn't have OwnedWriteHalf -- use full stream or split differently
  ```
- Replace `tokio::sync::oneshot` with `crate::runtime::channel::oneshot`
- For OwnedWriteHalf: check if it can be abstracted. If monoio doesn't have split streams, cfg-gate the entire send_rdb_to_replica function body.

**replication/replica.rs (3 errors):**
- Replace `tokio::io::{AsyncReadExt, AsyncWriteExt}` with cfg-gated imports
- Replace `tokio::net::TcpStream` with cfg-gated import
- Replace `tokio::time::{sleep, Duration}` -- Duration is std, sleep needs runtime abstraction:
  ```rust
  use std::time::Duration;
  // Use RuntimeTimer::sleep() or cfg-gate tokio::time::sleep vs monoio::time::sleep
  ```

**replication/state.rs (1 error):**
- Check and replace the single tokio import (likely a channel or type)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/rust-redis && cargo build 2>&1 | tail -5 && cargo test --lib persistence --lib replication 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - No unconditional tokio imports in persistence/ or replication/ files
    - All tokio::select! sites cfg-gated
    - TCP types (TcpStream, OwnedWriteHalf) cfg-gated
    - `cargo build` succeeds with default features
  </acceptance_criteria>
  <done>Persistence and replication subsystems migrated to runtime-agnostic types</done>
</task>

<task type="auto">
  <name>Task 2: Migrate cluster, blocking, pubsub, tracking, and ACL subsystems</name>
  <files>src/cluster/bus.rs, src/cluster/gossip.rs, src/cluster/failover.rs, src/blocking/mod.rs, src/pubsub/subscriber.rs, src/tracking/mod.rs, src/acl/io.rs</files>
  <read_first>
    - src/cluster/bus.rs lines 1-50 (imports and struct defs)
    - src/cluster/bus.rs lines 30-100 (select! in accept loop and handler)
    - src/cluster/gossip.rs lines 1-20 (imports)
    - src/cluster/gossip.rs lines 350-380 (select! in gossip ticker)
    - src/cluster/failover.rs lines 1-30 (imports: AsyncWriteExt, TcpStream)
    - src/blocking/mod.rs lines 1-10 (imports)
    - src/blocking/mod.rs lines 40-60 (BlockWaiter with oneshot)
    - src/pubsub/subscriber.rs lines 1-5 (imports)
    - src/tracking/mod.rs lines 1-10 (imports)
    - src/acl/io.rs (full file -- uses tokio::fs for async file I/O)
  </read_first>
  <action>
**cluster/bus.rs (7 errors):**
- Replace `tokio::io::{AsyncReadExt, AsyncWriteExt}` with cfg-gated imports
- Replace `tokio::net::{TcpListener, TcpStream}` with cfg-gated imports (monoio equivalents)
- Replace `use tokio_util::sync::CancellationToken;` with `use crate::runtime::cancel::CancellationToken;`
- Replace `tokio::select!` with cfg-gated select blocks (2 sites)

**cluster/gossip.rs (6 errors):**
- Replace `tokio::io::{AsyncReadExt, AsyncWriteExt}` with cfg-gated imports
- Replace `tokio::net::TcpStream` with cfg-gated import
- Replace `use tokio_util::sync::CancellationToken;` with `use crate::runtime::cancel::CancellationToken;`
- Replace `tokio::select!` with cfg-gated select (1 site)

**cluster/failover.rs (10 errors):**
- Replace `tokio::io::AsyncWriteExt` with cfg-gated import
- Replace `tokio::net::TcpStream` with cfg-gated import
- Check for tokio::sync channel usage and replace

**blocking/mod.rs (4 errors):**
- Replace `tokio::sync::oneshot::Sender<Option<Frame>>` in BlockWaiter with `crate::runtime::channel::OneshotSender<Option<Frame>>`
- Replace `tokio::sync::oneshot::channel()` calls in tests with `crate::runtime::channel::oneshot()`
- Update the type aliases throughout

**pubsub/subscriber.rs (1 error):**
- Replace `use tokio::sync::mpsc;` with `use crate::runtime::channel;`
- Update Sender/Receiver types

**tracking/mod.rs (1 error):**
- Replace `use tokio::sync::mpsc;` with `use crate::runtime::channel;`
- Update Sender/Receiver types
- Also check the test module (line 157) for tokio::sync::mpsc usage

**acl/io.rs (2 errors):**
- Replace `use tokio::fs;` and `use tokio::io::{AsyncBufReadExt, BufReader};`
- ACL file loading/saving is synchronous in practice (called during startup/command). Replace with `std::fs` and `std::io::BufReader`:
  - `tokio::fs::read_to_string` -> `std::fs::read_to_string`
  - `tokio::fs::write` -> `std::fs::write`
  - If the functions are async, change to sync or wrap in spawn_blocking
  - Check if callers await these -- if so, make them sync and remove async (ACL file I/O is rare, not hot path)
  </action>
  <verify>
    <automated>cd /Users/tindang/workspaces/tind-repo/rust-redis && cargo build 2>&1 | tail -5 && cargo test --lib cluster --lib blocking --lib pubsub --lib tracking --lib acl 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - No unconditional tokio imports in cluster/, blocking/, pubsub/, tracking/, acl/ files
    - All tokio::select! sites cfg-gated
    - Cluster TCP types cfg-gated
    - ACL file I/O uses std::fs (runtime-agnostic)
    - `cargo build` succeeds with default features
  </acceptance_criteria>
  <done>All remaining subsystems migrated to runtime-agnostic types</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds
- `cargo test` passes all existing tests
- `grep -rn "use tokio::" src/ --include="*.rs" | grep -v "#\[cfg" | grep -v "//\|tests\|test"` shows no unconditional tokio imports outside cfg blocks or test modules
</verification>

<success_criteria>
- All 152 compile errors from tokio type imports eliminated
- Every subsystem uses runtime-agnostic channels, cancellation, and cfg-gated TCP/IO
</success_criteria>

<output>
After completion, create `.planning/phases/31-full-monoio-migration-replace-tokio-channels-select-tcp-codec-spawn-across-30-files-for-complete-runtime-swap/31-03-SUMMARY.md`
</output>
