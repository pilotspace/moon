# WS15-durability-followups — PLAN (wave 2, part 3): cold-tier / spill / BGSAVE-status durability
personas: `.add/personas/storage-durability-engineer.md` (lead: every change here is a durability change — prove recovery, not just the live reply) · `.add/personas/ci-test-integrity-engineer.md` (red→green regression tests, kill-9 recovery)

Base: main `ae21476` (PR #1221 + PR #1227 merged). Baseline binary: `/home/user/wt/bin/baseline-ae21476`. Ports 7320–7339.
Read first: `plans/WS6-persistence/NOTES.md` (moon#1202 spill-withdraw analysis (a)–(c)), `plans/WS12-snapshot-integrity/NOTES.md` (fold / COW epoch design), `src/persistence/aof/fold_stream.rs`, `src/storage/tiered/cold_index.rs`, `src/storage/db/cold_replay_gate.rs`.
Order: #1215 and #1223 share the fold-cut / cold-manifest machinery — design them TOGETHER in NOTES.md before coding either; land #1230 whenever convenient (independent).

## Issues (bugs first, in this order)
1. **moon#1223 (P1 data loss)** a spill withdrawn under AOF backpressure (moon#1202, `rehydrate_unpublished_spill` in `src/shard/persistence_tick.rs:~883-903`) after a BGREWRITEAOF fold whose base image streamed from the hot table only (`src/persistence/aof/fold_stream.rs:~147`) leaves the key in NO durable artifact.
   - Preferred fix (small): when the `MOON.SPILLED` marker is refused, PUBLISH the spill without the marker (manifest-listed cold file) instead of withdrawing, at least when the spill file id is below the latest fold's cut — value-correct whether the fold commits or aborts per #1202's (a)–(c); do NOT count it as a lost acknowledged append in AOF status. Heavier alternative: include in-flight `PendingSpill` payloads in the fold base image. Choose with a written argument in NOTES.md.
   - Regression test: the review's proof test is at `/home/user/wt/handoff/1223-rv-integ-tests-persistence_tick.rs` (`rv_integ_withdrawn_spill_after_fold_has_a_durable_source`, fails on `ae21476` with `live=Some("acked-value") in_base=false manifest_lists_file=false in_post_fold_log=false`) — adopt it (adapt paths), plus a real-server kill-9 recovery test, both runtimes.
2. **moon#1230 (BUG)** `rdb_last_bgsave_status` stays `err` forever after one failed sharded BGSAVE (`src/command/persistence.rs:190` `bgsave_shard_done` only ever stores `false`). Store `true` when every shard of a sharded save succeeds, `false` only for the failed save; check the single-shard path and both runtimes; `rdb_last_save_time` advances on success. Integration test: force one failed save (FLUSHALL during a large BGSAVE — moon#1224 abort), see `err`, clean BGSAVE, see `ok`. Then simplify `tests/perf_ws12_bgsave_split.rs::an_aborted_bgsave_cannot_corrupt_the_next_one` to trust the field if that is now safe.
3. **moon#1215 (P0, data resurrection, pre-existing)** a DELETED cold-tier key resurrects after an AOF rewrite + ANY restart (clean SHUTDOWN suffices), default config. Mechanism (issue): DEL of a cold key only updates memory (`remove_counting_cold` → `ColdIndex::remove`, `src/storage/db/kv_ops.rs`, `src/storage/tiered/cold_index.rs`); the rewrite drops the only record of the delete and the new `MOON.COLDCUT` approves every older cold file whole (`src/storage/db/cold_replay_gate.rs`); boot re-indexes every slot of every Active file; the `TOMBSTONE` flag in `src/persistence/kv_page.rs` is never written nor read.
   - FIRST commit: cherry-pick the proof tests from `origin/test/cold-del-rewrite-regression` (commit `2eadbb1`: `src/storage/tiered/cold_del_rewrite_tests.rs`, `src/storage/tiered/mod.rs` mod line, `tests/crash_recovery_cold_del_rewrite.rs` — 8 real-server cases, `#[ignore]`d like the other crash suites; run with `MOON_BIN=<bin> cargo test --release --test crash_recovery_cold_del_rewrite -- --ignored --test-threads 1`). Confirm they are red on `ae21476`.
   - Fix: choose among (a) a per-slot delete record that survives rewrites (`MOON.COLDDEL <file_id> key…` after the cut), (b) writing the existing `TOMBSTONE` flag into the spill page (checksum updated, fsynced before the rewrite commits) and honouring it in the rebuild, (c) compacting files that contain deleted slots before the cut. PREFER a format-neutral option; if the one you pick changes what older binaries read, version-gate it and add a backward-compat test (old files still load). Write the choice + argument (incl. crash windows between each step and FLUSHDB of cold keys) in NOTES.md. Un-ignore the recovery-level test; the real-server crash suite stays `#[ignore]` (house convention) but must PASS when run with `--ignored` on your binary — paste the result in SUMMARY.
   - Must not regress moon#1223's fix or moon#1202 (spill withdraw); interaction of all three with a fold in flight is the core of your NOTES.

## Owned files
`src/shard/persistence_tick.rs`, `src/persistence/**` EXCEPT the replay readers WS10 owns (`aof_manifest/shard_replay.rs`, `aof/mod.rs` replay reader, `replay.rs`), `src/storage/tiered/**`, `src/storage/db/cold_replay_gate.rs`, the cold branch of `remove_counting_cold` in `src/storage/db/kv_ops.rs` (WS10 owns `get` in the same file — touch only the cold-removal path), `src/command/persistence.rs`, tests `tests/perf_ws15_*.rs` + the two cherry-picked #1215 test files.

## Not yours
`src/server/conn/**` (WS7), `src/shard/**` other than `persistence_tick.rs` (WS8), collection write sites / listpack / the rest of `src/storage/db/**` (WS10), the moon#1228 capture sites (WS16, queued).
