[package]
name = "rust-redis"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.10"
thiserror = "2.0"
tikv-jemallocator = "0.6"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "resp_parsing"
harness = false
